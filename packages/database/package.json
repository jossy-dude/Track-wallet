{
  "name": "@omni-sync/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@omni-sync/core": "0.1.0",
    "zustand": "^4.5.5"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --pool=threads --poolOptions.threads.singleThread",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
