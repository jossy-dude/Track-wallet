{
  "name": "@omni-sync/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "test": "node -e \"console.log('No desktop tests yet')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@omni-sync/core": "0.1.0",
    "@omni-sync/database": "0.1.0",
    "@omni-sync/ui": "0.1.0",
    "@tauri-apps/api": "^2.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
