# Desktop Tauri Host

This folder is intentionally dormant during the first mobile-first scaffold.
The React desktop shell exists now so the monorepo shape is stable before the
native Tauri host is initialized in a later slice.

