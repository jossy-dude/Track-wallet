# Work Log

## 2026-05-01
- Replaced the old inline mobile settings slab with a navigation-first settings hub in `apps/mobile/src/screens/SettingsScreen.tsx`.
- Added dedicated mobile settings detail pages for appearance, security, parsing, SMS forwarding, sync, device pairing, help, and advanced runtime controls in `apps/mobile/src/screens/SettingsDetailScreen.tsx`.
- Replaced the old `insights` tab with a transaction-first `ledger` tab and replaced the old account carousel screen with an account-management page in `apps/mobile/src/screens/LedgerScreen.tsx` and `apps/mobile/src/screens/CardsScreen.tsx`.
- Updated the mobile shell in `apps/mobile/src/App.tsx` so settings routes can open nested pages and return from `Connect Device` back into `Sync`.
- Verified mobile UI changes with `npm run test -w @omni-sync/mobile` and `npm run build -w @omni-sync/mobile`.
- Added a code-aware mobile UX audit for the settings/accounts/ledger redesign in `docs/ai/2026-05-01-mobile-ux-audit-settings-accounts-ledger-redesign.md`.
- Turned Help into an actual documentation surface with category guides plus realistic FAQ accordions in `apps/mobile/src/screens/settingsHelpContent.ts` and `apps/mobile/src/screens/SettingsDetailScreen.tsx`.
- Added save/check animations, local status toasts, a focused parser editor overlay, and a stateful code-based device pairing flow in `apps/mobile/src/screens/SettingsDetailScreen.tsx`.
- Wired sync management actions into the real mobile store so nearby pairing, primary-route promotion, trusted-device removal, and manual refresh all do something visible from the UI.
- Downgraded misleading settings/account credibility chrome by removing fake live status/profile signals and relabeling accounts toward tracked balances instead of net worth.
- Added a first-class `Data & Storage` mobile settings page in `apps/mobile/src/screens/DataStoragePage.tsx`, wired it into the hub, and made browser-safe export, backup, restore, and historical-import staging actions real while keeping duplicate review, reconciliation, and native SQLite paths clearly marked as coming soon.
- Added local import-policy preferences for `merge | replace | backup_then_replace`, duplicate behavior, and recent-days review defaults in `apps/mobile/src/preferences/storagePreferences.ts`.
- Verified the new `Data & Storage` surface with `npx tsc -p apps/mobile/tsconfig.json --noEmit`; the full Vite build is still blocked by the existing Windows `spawn EPERM` environment issue.
- Reworked the mobile parser settings surface into a tabbed parser workspace with template groups, sandbox testing, diagnostics, parser-pack import/export controls, and a focused editor flow in `apps/mobile/src/screens/SettingsDetailScreen.tsx`.
- Added real local parser-workspace hydration/persistence in `apps/mobile/src/preferences/parserPreferences.ts` so parser templates and parser-control toggles now survive page reloads instead of only living in-screen state.
- Updated parser settings coverage in `apps/mobile/src/screens/SettingsDetailScreen.test.tsx` and verified the screen with `cmd /c npx tsc -p apps/mobile/tsconfig.json --noEmit` plus `cmd /c npm run test -w @omni-sync/mobile -- SettingsDetailScreen.test.tsx`.

## 2026-05-02
- Added release-prep CI hygiene for the Android alpha lane by creating `.github/workflows/ci-baseline.yml` with explicit `typecheck`, `unit-tests`, and `mobile-web-build` jobs that only target checks the repo can honestly run today.
- Added root `ci:typecheck`, `ci:test`, and `ci:build:mobile` scripts in `package.json` so GitHub Actions does not rely on the broader root `test` or `lint` labels as release signals.
- Documented the current gate truth, alpha checklist, and intentional Android-build deferrals in `docs/release/ANDROID_INTERNAL_ALPHA_RUNBOOK.md`.
- Remapped the `Data & Storage` mobile settings page in `apps/mobile/src/screens/DataStoragePage.tsx` onto the user-provided management layout with `Database Management`, storage health, `Data Integrity`, and `Logging` cards while preserving the existing export, backup, restore, import, and destructive-clear behavior.
- Kept the deeper `Historical Import`, reported-balance tracking, reconciliation placeholder, and native-authority boundary panels intact so the redesign stays honest about what is real versus still backend-dependent.
- Re-verified the parser/storage settings slice with `cmd /c npm run test -w @omni-sync/mobile -- SettingsDetailScreen.test.tsx` and `cmd /c npx tsc -p apps/mobile/tsconfig.json --noEmit`.
- Added a shared account-authority state slice in `packages/core/src/types.ts`, `packages/database/src/account-management.ts`, and `packages/database/src/transaction-store.ts` for custom accounts plus persisted hide-state overrides, so mobile account screens no longer need to invent that contract locally before UI wiring lands.
- Added the persisted SMS capture backend/store slice across `packages/core/src/types.ts`, `packages/database/src/sms-capture-management.ts`, and `packages/database/src/transaction-store.ts` with exact-sender allowlist rules, durable queue status tracking, merge-and-dedupe support for native snapshots, parser-drain actions, and diagnostics counters for filtered, duplicate, queued, unmatched, and failed outcomes.
- Added a dedicated workspace Capacitor plugin package at `packages/mobile-sms-capture` plus `apps/mobile/src/sms/smsCaptureBridge.ts` so Android SMS capture can persist a native queue, expose permission/status snapshots to JS, and emit live `smsCaptured` events without faking support in the current no-host preview state.
- Added shared storage/import contracts in `packages/core/src/storage.ts` for finance snapshots, historical import packages, backups, parser-aware import summaries, and system log entries so storage flows no longer depend on page-local JSON shapes.
- Hardened `packages/database/src/transaction-store.ts` so export, backup, restore, staged import, applied import, failed import, and clear-local-data all emit real persisted system log events, and so historical import logs carry parser-template usage/unknown-template warnings without reparsing the drafts.
- Fixed the long-standing launch-readiness gaps in `apps/mobile/src/preferences/storagePreferences.ts` and `apps/mobile/src/screens/DataStoragePage.tsx`: duplicate-mode preferences now preserve `review`, backup restore no longer forces `duplicateMode` back to `skip`, historical import staging consults the runtime parser template catalog, and the system logs page now reads the real store-backed event trail instead of local placeholder entries.
- Adjusted `packages/database/src/mockData.ts` so budget summaries include `income` only when imported or approved transactions actually contain income rows, which fixes the historical-import store assertion without permanently adding an empty income card to non-income states.
- Verified the launch-hardening slice with `cmd /c npm run test -w @omni-sync/core -- src/storage.test.ts`, `cmd /c npm run test -w @omni-sync/database -- src/transaction-store.test.ts`, `cmd /c npm run test -w @omni-sync/mobile -- src/preferences/storagePreferences.test.ts src/screens/SettingsDetailScreen.test.tsx`, and `cmd /c npm run build -w @omni-sync/mobile`.
- Extended the SMS capture backend slice with a persisted backfill-intent request, a runtime-snapshot apply action for the Android bridge seam, and a backend `selectSmsCaptureRoutingSummary` selector so the upcoming capture/setup page can render honest manual-only, blocked, native-active, queue, and sender-rule states without inventing local UI state.
- Hardened the Android alpha showcase slice in `apps/mobile/src/App.tsx`, `apps/mobile/src/screens/IntroOnboardingScreen.tsx`, `apps/mobile/src/screens/SettingsDetailScreen.tsx`, and `apps/mobile/src/components/accountCardStudio.tsx` so showcase mode is explicit, reloads into a truly separate sample workspace, and no longer leaks nested-button warnings through the stacked home account cards.
- Replaced misleading mobile alpha copy and actions by removing the fake profile-image picker action, renaming the top-bar demo control to `Showcase`, updating onboarding/private-wallet wording, and scrubbing `local preview route` / desktop-promise language from the active mobile pairing and sync surfaces where this slice owns the UI.
- Re-verified the showcase hardening slice with `cmd /c npm run test -w @omni-sync/mobile -- src/App.test.tsx src/screens/IntroOnboardingScreen.test.tsx src/screens/SettingsScreen.test.tsx src/screens/SettingsDetailScreen.test.tsx`.
- Finished the mobile SMS runtime seam by adding `apps/mobile/src/sms/useSmsCaptureRuntime.ts`, normalizing native snapshot items into parser-ready queue work in `packages/database/src/transaction-store.ts`, wiring `apps/mobile/src/screens/SmsCaptureRoutingPage.tsx` and `apps/mobile/src/screens/ParsingWorkspacePage.tsx` to the real bridge/store state, and keeping historical SMS as an honest request-only path until host/native backfill exists.
- Verified the mobile SMS lane with `cmd /c npm run typecheck -w @omni-sync/mobile` plus `cmd /c npm run test -w @omni-sync/mobile -- src/sms/useSmsCaptureRuntime.test.tsx src/sms/smsCaptureBridge.test.ts src/screens/SettingsDetailScreen.test.tsx`; the parallel authority lane still blocks `cmd /c npm run test -w @omni-sync/database -- src/transaction-store.test.ts` because `packages/database/src/sqlite/state-storage` is not present yet.
- Landed the native authority lane in `packages/database` by replacing the throwing `native-sqlite-adapter` stub with a real SQLite-backed adapter, adding `packages/database/src/sqlite/state-storage.ts` for SQL.js document persistence plus legacy preview-state migration, and moving the singleton transaction store onto runtime-selected storage so native mobile now hydrates from SQLite instead of `localStorage`.
- Extended the storage contracts in `packages/core/src/storage.ts` and the mobile restore glue in `apps/mobile/src/screens/DataStoragePage.tsx` so exports/backups can carry native authority state for custom accounts, visibility overrides, SMS capture settings/queue, sync state, and system logs instead of only the finance snapshot.
- Added a hydration gate plus native runtime configuration in `apps/mobile/src/App.tsx` so demo seeding waits for authority rehydration and does not clobber persisted native state on startup.
- Verified the authority lane with `cmd /c npm run test -w @omni-sync/database -- src/authority/native-sqlite-adapter.test.ts src/transaction-store.test.ts`, `cmd /c npm run test -w @omni-sync/core -- src/storage.test.ts`, `cmd /c npm run typecheck -w @omni-sync/database`, `cmd /c npm run typecheck -w @omni-sync/mobile`, and `cmd /c npm run test -w @omni-sync/mobile -- src/App.test.tsx`.

## 2026-05-03
- Ran a repo-level product/release honesty pass across `README.md`, `docs/product/SOURCE_OF_TRUTH.md`, `docs/product/FRONTEND_BACKEND_HANDOFF.md`, and `docs/release/ANDROID_INTERNAL_ALPHA_RUNBOOK.md` so the docs stop implying a live desktop or sync-alpha lane that the current internal alpha does not ship.
- Earlier same-day coordinator snapshot for this pass recorded `npm run ci:typecheck` passed, `npm run ci:build:mobile` passed, `npm run test -w @omni-sync/core` passed, `npm run test -w @omni-sync/database` passed, and `npm run ci:test` still appeared red because mobile tests had drifted from the current UI copy/flow.
- Narrowed storage wording to the current truth: mobile uses a more advanced local authority path than browser preview storage, but the repo should not claim a finished SQLite-backed authority yet.
- Closed the stalled second-wave lane by wiring queue rejection into the live mobile review surfaces: `packages/ui/src/components/InboxTransactionItem.tsx`, `packages/ui/src/components/InboxPanel.tsx`, `apps/mobile/src/screens/HomeScreen.tsx`, `apps/mobile/src/screens/InboxScreen.tsx`, and `apps/mobile/src/App.tsx` now expose real `Reject` actions backed by `rejectQueueItem`.
- Made manual entry honest in `apps/mobile/src/App.tsx` by queuing new entries into the inbox review lane instead of auto-posting them straight to the ledger, and renamed the action to `Queue for review`.
- Extended the shared edit modal in `packages/ui/src/components/EditTransactionModal.tsx` so queued items can also be rejected directly from the editor surface while approved items keep a non-destructive close path.
- Reduced parser debug leakage in `apps/mobile/src/hooks/useParser.ts` by redacting verbose parser logging to metadata and result status instead of printing full raw SMS bodies.
- Tightened storage honesty in `apps/mobile/src/screens/DataStoragePage.tsx` by replacing stale `preview runtime` and `native database not wired` copy with local-first authority language and by clarifying that clear-local-data also removes finance-linked local preferences and asks the SMS bridge to clear runtime state.
- Added and aligned mobile verification coverage for the new budget workspace and app/setup flow in `apps/mobile/src/preferences/budgetPreferences.test.ts`, `apps/mobile/src/App.test.tsx`, `apps/mobile/src/sms/smsCaptureBridge.test.ts`, and `apps/mobile/src/sms/useSmsCaptureRuntime.test.tsx`.
- Fixed stale verification expectations in `packages/database/src/transaction-store.test.ts` so the corrected reviewed-food transaction matches the real seeded budget math (`149500` spent minor after edit).
- Earlier 2026-05-03 worker snapshot: the phase-3 gates reran successfully with `cmd /c npm run typecheck -w @omni-sync/mobile`, `cmd /c npm run ci:typecheck`, `cmd /c npm run ci:test`, `cmd /c npm run ci:build:mobile`, and `cmd /c npm run android:build:debug -w @omni-sync/mobile`.
- Hardened the Android alpha host manifest in `apps/mobile/android/app/src/main/AndroidManifest.xml` by disabling app backups (`android:allowBackup="false"`) so debug/alpha builds do not advertise finance-state backup behavior by default.
- Removed the root re-export of `packages/database/src/services/historical-import-service.ts` from `packages/database/src/index.ts`, promoted `buildHistoricalImportAccountKey` into the shared `@omni-sync/core` storage contract, and updated the mobile import flow so the app keeps the lazy historical-import chunk without the earlier mixed static/dynamic import warning.
- Cleaned the Ledger screen harness in `apps/mobile/src/screens/LedgerScreen.test.tsx` by setting `IS_REACT_ACT_ENVIRONMENT` explicitly during the test lifecycle, which removed the stale React `act(...)` warning from the full mobile suite.
- Removed the Android host wrapper `shell: true` path in `apps/mobile/android/tools/android-host.mjs` by switching Windows batch execution to an explicit `cmd.exe /d /s /c ...` invocation, which eliminates the previous Node child-process deprecation warning from `android:build:debug`.
- Confirmed there is no attached emulator or device available through `adb`, so this pass stops honestly at successful debug APK generation instead of claiming install-level Android QA.
- Tightened the SQL.js browser asset loader in `packages/database/src/sqlite/client.ts` to explicit wasm filenames instead of a dynamic `dist/${file}` URL, which removed the accidental asm/worker/debug asset fan-out from Vite builds and left the mobile bundle with a single wasm payload plus the lazy historical-import chunk.
- Re-verified the post-cleanup bundle and Android host path with `cmd /c npm run ci:build:mobile` and `cmd /c npm run android:build:debug -w @omni-sync/mobile`; both pass after the SQL.js narrowing and Android host wrapper cleanup.
- Later shared-workspace verification on 2026-05-03 turned the gate snapshot green: `npm run ci:typecheck`, `cmd /c npm run ci:test`, `cmd /c npm run ci:build:mobile`, `cmd /c npm run test -w @omni-sync/core`, and `cmd /c npm run test -w @omni-sync/database` all passed. The mobile production build still emits a non-blocking main-JS chunk-size warning (`~658 kB`), and the Android debug build still reports Gradle `flatDir` warnings from Capacitor/plugin wiring.

## 2026-05-04
- Updated `docs/release/ANDROID_INTERNAL_ALPHA_RUNBOOK.md` to the current Android internal alpha candidate truth: `cmd /c npm run ci:typecheck`, `cmd /c npm run ci:test`, `cmd /c npm run ci:build:mobile`, and `cmd /c npm run ci:build:android:release` are the green blocking gates for this release-doc pass.
- Recorded the current unsigned artifact truth for the candidate: `apps/mobile/android/app/build/outputs/apk/release/app-release-unsigned.apk` exists, and `apps/mobile/android/app/build/outputs/apk/release/output-metadata.json` reports `applicationId` `com.omnisync.mobile`, `versionCode` `2`, and `versionName` `0.1.0-alpha.1`.
- Added a concise `Ready For Internal Alpha` checklist to the runbook that marks the candidate ready for unsigned artifact sharing while keeping install-level QA explicitly pending.
- Logged the current Android QA blocker honestly: `adb` is present at `$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe`, but there are no attached devices, and no emulator binary exists at `$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe`.
- Recorded the remaining non-blocking warning truth for this candidate: the mobile production build still emits the chunk-size warning, and the Android release build still emits the Android SDK XML warning.
- Added an opt-in Android signing lane in `apps/mobile/android/app/build.gradle` that keeps default release builds unsigned, activates signing only when explicitly requested, and fails immediately with a clear missing-input message if the required keystore values are absent.
- Added repo-native Android host tooling in `apps/mobile/android/tools/android-host.mjs` and `apps/mobile/package.json` for `android:doctor`, `android:targets`, `android:install:debug`, and `android:smoke:debug`, then verified on the real host that SDK tools exist but no runnable device/emulator target is available.
- Replaced the shared runtime Google Fonts Material Symbols dependency with bundled local SVG rendering in `packages/ui/src/components/MaterialSymbol.tsx`, `packages/ui/src/components/materialSymbolRegistry.tsx`, and `packages/ui/src/theme.css`.
- Moved mobile security preference persistence out of plain `window.localStorage` and into the shared local authority path via `apps/mobile/src/preferences/securityPreferences.ts`, `packages/core/src/types.ts`, and `packages/database/src/transaction-store.ts`, while tightening the Settings security copy so it does not imply native biometric enforcement that does not exist yet.
- Re-verified the post-hardening state with `cmd /c npm run ci:typecheck`, `cmd /c npm run ci:test`, `cmd /c npm run ci:build:mobile`, `cmd /c npm run ci:build:android:release`, `cmd /c npm run android:doctor -w @omni-sync/mobile`, `cmd /c npm run android:targets -w @omni-sync/mobile`, `cmd /c npm run android:install:debug -w @omni-sync/mobile`, `cmd /c npm run test -w @omni-sync/mobile -- src/screens/SettingsDetailScreen.test.tsx`, `cmd /c npm run test -w @omni-sync/database -- src/transaction-store.test.ts`, and `cmd /c npm run test -w @omni-sync/ui`.
- Added `.github/workflows/android-alpha-prerelease.yml` so GitHub can rerun the Android alpha gates on tag push or manual dispatch, upload the APK plus `output-metadata.json`, and publish an explicitly labeled prerelease without pretending signing or device QA happened by default.
- Tightened `.github/workflows/ci-baseline.yml` so the Android artifact job now waits on `mobile-web-build`, runs with `TRACK_WALLET_RELEASE_SIGNING_ENABLED=false`, and keeps the baseline lane explicitly unsigned unless a separate release workflow is used.
- Closed the remaining mobile alpha-close integration blocker in `apps/mobile/src/screens/SmsCaptureRoutingPage.tsx` by aligning the backfill capability checks with the shared `unsupported` union while preserving the real request-only behavior for unsupported historical SMS capture.
- Restored release-gate test alignment after the late UX batches by making the floating `Add` action accessible in `packages/ui/src/components/BottomNavBar.tsx`, letting custom onboarding pages use generic next/complete controls in `apps/mobile/src/screens/IntroOnboardingScreen.tsx`, and updating the mobile regression tests to match the current onboarding/profile/setup copy without reverting the landed UX.
- Re-verified this final polish pass with `cmd /c npm run test -w @omni-sync/mobile -- src/screens/SmsCaptureRoutingPage.test.tsx`, `cmd /c npm run test -w @omni-sync/mobile -- src/App.test.tsx`, `cmd /c npm run ci:typecheck`, `cmd /c npm run ci:test`, `cmd /c npm run ci:build:mobile`, and `cmd /c npm run ci:build:android:release`; all passed on 2026-05-04 after the fixes.
